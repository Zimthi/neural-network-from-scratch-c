# neural-network-from-scratch-c
Implements forward propagation, sigmoid activation, mean squared error loss, backpropagation, and gradient descent entirely in C, using only math.h. Trained on the XOR problem as a first milestone toward a portfolio-ready neural network project.
